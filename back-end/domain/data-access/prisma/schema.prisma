generator client {
provider = "prisma-client-js"
}

datasource db {
provider = "postgresql"
url = env( "DATABASE_URL")
}

model User {
id Int @id @default(autoincrement())
name String
specialisation String
email String
password String
teams Team[]
}

model Team {
    id Int @id @default(autoincrement())
    users User[]
}